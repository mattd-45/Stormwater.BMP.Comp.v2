# Legacy Files

This folder contains retired or backup files from earlier versions of the stormwater BMP tool.

Active development should happen in:

- `v3/` for the current app shell and workflow code
- `data/` for active data tables
- `engine/` for the active model engine

Do not reference files in this folder from active V3 code unless a legacy comparison is intentionally needed.

